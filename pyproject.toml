[project]
name = "asteroids"
version = "0.1.0"
description = "Asteroids. A small project from boot.dev"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pygame==2.6.1",
]

[dependency-groups]
dev = [
    "ipykernel>=7.2.0",
]
